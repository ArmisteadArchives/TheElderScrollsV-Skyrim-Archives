<%= form_for(@ingredient_effect) do |f| %>
  <% if @ingredient_effect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient_effect.errors.count, "error") %> prohibited this ingredient_effect from being saved:</h2>

      <ul>
      <% @ingredient_effect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :effect %><br>
    <%= f.text_area :effect %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
